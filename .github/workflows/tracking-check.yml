name: Analytics tracking check

on:
  push:
  pull_request:

jobs:
  check-tracking:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/check-tracking.py
